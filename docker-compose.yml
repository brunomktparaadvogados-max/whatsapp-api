version: '3.8'

services:
  whatsapp-api:
    build: .
    container_name: whatsapp-api
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - HOST=0.0.0.0
      - SESSION_DIR=/app/sessions
    volumes:
      - ./sessions:/app/sessions
    restart: unless-stopped
